export enum WorkType {
    FULL_TIME,
    PART_TIME,
    CONTRACT,
    TEMPORARY,
    VOLUNTEER,
    INTERNSHIP,
}

export enum PriceType {
    FIXED,
    HOURLY,
}

export enum RequiredLevel {
    ENTRY,
    INTERMEDIATE,
    EXPERT,
}
