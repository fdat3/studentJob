export enum WorkType {
  CONTRACT,
  FULL_TIME,
  PART_TIME,
  TEMPORARY,
  VOLUNTEER,
  INTERNSHIP,
}

export enum PriceType {
  FIXED,
  HOURLY,
}

export enum RequiredLevel {
  ENTRY,
  INTERMEDIATE,
  EXPERT,
}
